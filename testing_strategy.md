Following Group B's Strategy, we will:

1. Document the expected functionality of the tests we perform
2. Connect to the target Pico over UART via the debugger Pico on the serial monitor
3. Test basic algebra, characters, operations, and blinking behavior using unit tests. The behavior will be observed, while the others will be tested with asserts from the Unity framework.
4. Discuss and investigate tests that (may) fail